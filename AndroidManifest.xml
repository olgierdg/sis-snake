<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="@string/app_name"
        android:theme="@style/AppTheme" android:icon="@drawable/ic_launcher">
        <activity
            android:name="game.MainActivity"
            android:label="@string/title_activity_main" >
        </activity>
        <activity
            android:name="game.MainMenu"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="game.ModeMenuActivity" android:label="@string/title_activity_mode_menu"></activity>
        <activity android:label="@string/settings" android:name="game.SettingsActivity"></activity>
        <activity android:label="@string/title_portals_menu" android:name="game.PortalsMenu"></activity>
        <activity android:label="@string/best_scores" android:name="game.BestScores"></activity>
    </application>

</manifest>